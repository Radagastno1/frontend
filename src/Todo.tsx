import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

export default function Todo() {
  return (
    <Box sx={{ backgroundColor: "pink", height: "100vh", width: "100%" }}>
      <Typography variant="h2">hej</Typography>
    </Box>
  );
}
